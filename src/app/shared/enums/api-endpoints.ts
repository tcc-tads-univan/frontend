export enum ApiEndpoints {
  CAMPI = "/carpool/Campi",
  RIDE = "/carpool/Ride",
  SCHEDULE = "/carpool/Schedule",
  DRIVER = "/driver",
  STUDENT = "/student",
  LOGIN = "/login",
  SUBSCRIPTION = "/subscription"
}
